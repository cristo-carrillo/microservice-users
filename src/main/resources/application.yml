server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/usuarios
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: microservice-users

appDescription: "Pragma micro_users"
appVersion: "1.0.0"
